<?xml version="1.0" encoding="UTF-8"?>

<Shell
	x:Class="MauiDemoApp.AppShell"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:local="clr-namespace:MauiDemoApp"
	FlyoutBehavior="Disabled">

	<TabBar>
		<ShellContent
			Title="Demo App"
			ContentTemplate="{DataTemplate local:MainPage}"
			Route="MainPage" />

		<ShellContent
			Title="Flutter App (Renderer)"
			ContentTemplate="{DataTemplate local:FlutterPage}"
			Route="FlutterPage" />
		
		<ShellContent
			Title="Flutter App (Handler)"
			ContentTemplate="{DataTemplate local:FlutterHandlerPage}"
			Route="FlutterPage2" />
	</TabBar>
</Shell>